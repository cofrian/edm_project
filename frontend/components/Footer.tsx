export function Footer() {
  return (
    <footer className="border-t border-slate-200 bg-white">
      <div className="mx-auto max-w-7xl px-4 py-6 text-sm text-slate-500">
        <p>
          <span className="font-semibold text-slate-700">UrbanFlow Valencia</span> ·
          Entrega EDM · CatBoost + FastAPI + Next.js + PuLP
        </p>
        <p className="mt-1">
          Predicción de tráfico → evaluación → optimización → despliegue y monitorización.
        </p>
      </div>
    </footer>
  );
}
